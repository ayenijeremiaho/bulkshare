enum role {
    BUYER,
    SELLER,
    ADMIN,
    SUPER_ADMIN
}

enum status {
    PENDING,
    UNVERIFIED,
    VERIFIED,
}

enum productState {
    AVAILABLE,
    OUT_OF_STOCK,
}

enum measurementType {

}

export {role, status}